export type ConfigValue = {
  serverUrl: string;
};

// ----------------------------------------------------------------------

export const CONFIG: ConfigValue = {
  serverUrl: 'http://localhost:8000',
};
